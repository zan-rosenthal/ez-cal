import React from "react";
import Dialog from "modules/generic/components/Dialog";

export default function AppointmentErrorDialog({ error }) {
  return (
    <Dialog active>
      Hello
    </Dialog>
  );
}
